[workspace]
members = [
  "math",
  "crypto",
  "common",
  "fri",
  "prover",
  "verifier",
  "examples",
  "fractal_indexer"
]

[profile.release]
codegen-units = 1
lto = true

[profile.bench]
codegen-units = 1
lto = true
