[package]
name = "common"
version = "0.1.0"
edition = "2018"

[lib]
bench = false

[dependencies]
rand = "0.7.3"
math = { path = "../math" }
crypto = { path = "../crypto" }
serde = { version = "1.0.115", features = ["derive"] }